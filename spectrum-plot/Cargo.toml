[package]
name = "spectrum-plot"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.89"
# TODO: why do we need to depend on a test feature?
pitch-detector = { path = "../pitch-detector", features = ["test_utils", "plot"]}

